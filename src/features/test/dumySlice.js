import { createSlice, createAsyncThunk } from '@reduxjs/toolkit'

const scliceName = 'dumy'
const initialState = []


const dumySckice = createSlice({
    name:scliceName,
    initialState,
    reducers: {
        // Reducer comes here
    },
    extraReducers: {
         // Extra reducer comes here
    },
})